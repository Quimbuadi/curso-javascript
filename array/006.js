let alunos = [
    {'nome':'quimbuadi', 'idade': 5},
    {'nome': 'samuel', 'idade': 18},
    {'nome': 'correia', 'idade': 35}
];
/**
 * alunos.forEach(function(value, index){
    if(value.nome == "josue"){
        console.log("está no index", index);
    }else{
        console.log('não existe');
    }
})
 */


